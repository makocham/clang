#include<stdio.h>
int (int a, int b);
main()
{
	int a, b, kotae;
	printf("®”‚ğ2‚Â“ü—Í :");
	scanf("%d%d", &a, &b);
	kotae = (a, b);
	printf("‡Œv‚Í %d ‚Å‚·\n", kotae);
}

int (int a, int b)
{
	int ans;
	ans = a + b;
	return(ans);
}