#include<stdio.h>
main()
{
	int r = 0;
	int	sum = 0;
	int cnt = 0;

	do 
	{
		sum += r;
		cnt++;

		printf("®”(-999‚ÅI—¹)");
		scanf("%d", &r);

	} while (r != -999);

	cnt--;

	printf("‡Œv=%d\n");
	printf("•½‹Ï=%f\n", (float)sum / (float)cnt);
}